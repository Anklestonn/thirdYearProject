Original decryption idea:
	same as original. use python and C/Rust.
	use several ciphers: AES, PROTON/PHOTON(tiny one), RSA, tiny assymetric
	do tests on several metrics(single core, multicore,limited single core )

a honeypot that logs the max amount info

carving and extracting data from second hand computers

malware
